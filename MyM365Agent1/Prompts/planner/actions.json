[
    {
        "name": "createSourcingProject",
        "description": "PRIMARY ACTION: Create a sourcing project using text-based input. Use this IMMEDIATELY when user mentions: 'create', 'new project', 'start project', 'make project', 'sourcing project', or ANY intent to create a project. Parse project details from natural language input.",
        "parameters": {
            "type": "object",
            "properties": {
                "input": {
                    "type": "string",
                    "description": "User's project details including title, description, email address, start/end dates, and budget"
                }
            },
            "required": [
                "input"
            ]
        }
    },
    {
        "name": "showProjectForm",
        "description": "DISABLED ACTION: Do not use this action. It has been disabled in favor of text-based project creation. Always use createSourcingProject instead.",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    {
        "name": "submitProjectForm",
        "description": "Process submitted adaptive card form data to create a sourcing project. This is automatically triggered when user submits the project form.",
        "parameters": {
            "type": "object",
            "properties": {
                "projectTitle": {
                    "type": "string",
                    "description": "Project title from the form"
                },
                "description": {
                    "type": "string",
                    "description": "Project description from the form"
                },
                "email": {
                    "type": "string",
                    "description": "Email address from the form"
                },
                "startDate": {
                    "type": "string",
                    "description": "Start date from the form"
                },
                "endDate": {
                    "type": "string",
                    "description": "End date from the form"
                },
                "budget": {
                    "type": "number",
                    "description": "Budget amount from the form"
                }
            },
            "required": ["projectTitle", "description", "email", "startDate", "endDate", "budget"]
        }
    },
    {
        "name": "cancelProjectForm",
        "description": "Handle cancellation of the project form by the user",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    {
        "name": "upsertMilestones",
        "description": "Add or update project milestones and deliverables for an existing sourcing project",
        "parameters": {
            "type": "object",
            "properties": {
                "input": {
                    "type": "string",
                    "description": "User's milestone details with titles and delivery dates"
                }
            },
            "required": [
                "input"
            ]
        }
    },
    {
        "name": "resetWorkflow",
        "description": "Reset the entire workflow and start over from the beginning",
        "parameters": {
            "type": "object",
            "properties": {
                "reason": {
                    "type": "string",
                    "description": "Optional reason for resetting the workflow"
                }
            }
        }
    }
]